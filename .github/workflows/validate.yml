name: Validate local wiki

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse and validate PowerShell and browser sources
        shell: powershell
        run: .\Tests\Test-Repository.ps1
